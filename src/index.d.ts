interface Currency {
    name: string,
    value: number,
    flag: string,
    symbol: string,
}

interface SnackbarOptions {
    text: string;
    duration: number;
    // Add more properties as needed
  }